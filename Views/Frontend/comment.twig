{% extends "template.twig" %}
{% block title %}
{% set title = 'Commentaires' %} 
{{ title }}
{% endblock %}
{% block head %}
    {{ parent() }}
    
{% endblock %}
{% block body %}
    {% if _flash.getFlashMessage('succes') %}
    <div class="alert alert-success mt-4" role="alert">
        {{ _flash.getFlashMessage('succes') }}
    </div>
    {% endif %}
    <header>
        <ul class="navbar navbar-dark bg-primary d-flex justify-content-around">  
            <li class="nav-item list-inline-item">
            <a class="navbar-brand" href="/blog/">Accueil</a></li>
            <li class="nav-item list-inline-item">
            <a class="navbar-brand" href="/blog/registration">Inscription</a></li>
        </ul>    
    </header>    
    <a class="ml-3 mb-1 btn btn-outline-primary" href="/blog/posts">Retour à la liste des posts</a></p>
    <div class="card">
        <h3 class="card-header">{{ postview.title|upper }}</h3>
        <div class="card-body">
            <p class="card-text">{{ postview.content }}</p>    
        </div>
        <div class="card">
            <h3 class="card-header">Commentaires</h3>
            {% for comment in commentsview %}
            <div class="card-body">
                <p>{{ comment.comment }}</p>
                <p>le {{ comment.comment_date }} écrit par
                {{ comment.author }} 
                    </p>
            </div>
            {% endfor %}
        </div>
        <div class="mx-4 my-3 px-4 py-4 border border-primary">
            <p>Ecrire un commentaire</p>
            <form class="" action="/blog/commentadd" method="POST" >
                <input type="hidden" name="postId" id="" value="{{ postview.id }} " />
                <div class="form-group">
                    <label for="author">Auteur</label><br />
                    <input type="text" id="author" name="author" class="form-control" />
                </div>
                <div class="form-group">
                    <label for="comment">Commentaire</label><br />
                    <textarea id="mytextarea" name="comment" class="form-control"></textarea>
                </div>
                <div>
                    <input type="submit" class="btn btn-primary"/>
                </div>
            </form>
        </div>
    </div>
    <footer class="text-white bg-primary mt-3 card-group">
        <div class="card-body">
            <p>Bienvenue sur mon blog professionel, vous pouvez voir nos posts <a class="text-success" href="/blog/posts">ici</a></p>
        </div>
        <div class="card-body">
            <div>
                <script src="https://platform.linkedin.com/in.js" type="text/javascript">lang: en_US</script>
                <script type="IN/Share" data-url="linkedin.com/in/thomas-da-silva-seabra"></script>
            </div>
            <div>
                <a class="text-success" href="Public/CVtype.pdf.zip">Mon CV est disponible ici!</a>
            </div>
        </div> 
        <div class="card-body">     
        <a class="text-success" href="/blog/connect">Connexion</a>
        </div>
    </footer>
{% endblock %} 